export enum  COMMON_ROUTES {
    HOME = '/',
    ABOUT = '/about',
    CONTACT = '/contact',
    LOGIN = '/login',
    NOT_FOUND = '/not-found',
}

export enum  AUTH_USER_ROUTES {
  CALENDAR= '/calendar',
  DAY = '/day',
  TASK = '/task',
  PROFILE = '/profile',
}

export enum  ADMIN_ROUTES {
  USERS = '/users',
  EVENTS = '/events',
  SETTINGS = '/settings',
}